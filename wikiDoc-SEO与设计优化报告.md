# wikiDoc SEO 与设计优化执行报告

> 执行时间：2026-09-10
> 范围：第一批（小投入大收益）+ 第二批（需设计决策）
> 验证：`yarn build` EXIT=0，生成 435 个页面

---

## 一、第一批：技术 SEO 与内容质量

| # | 项目 | 改动 | 文件 | 状态 |
|---|---|---|---|---|
| 1 | robots.txt | 新建：Allow 全站、Disallow `/search` 与 `/tags/`、声明 sitemap | `static/robots.txt` | ✅ |
| 2 | 默认分享图 | 生成 1200×630 PNG（暗色 + 品牌绿），接入 `themeConfig.image` | `static/img/og-default.png` | ✅ |
| 3 | `og:type` / `og:site_name` | 通过 `headTags` 注入 | `docusaurus.config.js` | ✅ |
| 4 | `theme-color` | 移动端浏览器 UI 配色 `#0bb45b` | `docusaurus.config.js` | ✅ |
| 5 | 图片 alt | **329/329 全部合格**（原 317 文件名 + 8 空 + 4 其他） | 全站 MDX | ✅ |
| 6 | 重复 title 消歧 | 3 组 ×2 组内重名 | `exness-agent/{标准,先锋,美分}账户.mdx` | ✅ |
| 7 | 正文双 H1 | 6 篇修复（5 删除冗余 H1 + 1 降为 H2） | `指数交易.mdx`、`nytimes/*` | ✅ |

### 图片 alt 生成策略
优先级：**图片下一行说明文字 → 最近前置标题 → 文档标题 → 文件名清洗**。

### 重复 title 消歧依据
`exness-agent` 版讲**合作伙伴佣金计算**，`exness-trader` 版讲**账户本身**，因此前者改为：
- `标准账户（合作伙伴佣金）`
- `先锋账户（合作伙伴佣金）`
- `美分账户（合作伙伴佣金）`

### 顺带修复（仓库原有缺陷）
发现 2 个文件存在**孤立 CR**（`\r` 而非 `\r\n`），使 frontmatter 变成
`title: xxx\rdescription: ...`，YAML 解析异常。已修复：
- `docs/nytimes/nytimes.mdx`
- `docs/exness-agent/exness-agent.mdx`

（`git show HEAD` 证实提交版本中即存在，非本次引入。）

---

## 二、第二批：设计层优化

### 1. 首页重构
- 新建 `src/components/Homepage/`（`index.js` + `styles.module.css`）
- 结构：**Hero**（标题 + 主张 + 两个主 CTA）→ **3 张分类卡片**（客户帮助 / 代理帮助 / AI 与工具）→ **常用入口**（6 个高频文档）
- `docs/index.mdx` 改为 `<Homepage />`，在 `src/theme/MDXComponents.js` 注册
- 全部使用 Infima CSS 变量，自动适配明暗主题

### 2. 页脚启用
`themeConfig.footer`（此前整段被注释）：
- **文档分类**：客户帮助 / 代理帮助 / AI 与工具 / 纽时播报
- **常用入口**：账户类型对比、MT4/MT5 对比、出入金时间、佣金框架
- **其他**：站内搜索、站点地图、robots.txt
- **版权**：`CC-BY-SA 4.0 © 2026 WikiDoc · wiki.ssgg.net`

### 3. 侧栏分类重构
`sidebars.js` 由单一 `autogenerated` 改为**显式分类树**：

```
EXNESS 客户帮助（359 篇）
  ├ 账户类型与选择 (34)      ├ 交易平台与工具 (75)
  ├ 账户管理与安全 (41)      ├ 交易操作与订单 (31)
  ├ 入金与出金 (73)          ├ 交易时间与市场规则 (16)
  ├ 交易品种与合约 (57)      ├ 错误与故障排除 (23)
  └ 其他 (8)
EXNESS 代理帮助（57 篇）
  ├ 计划与入门 (22)  ├ 佣金与返佣 (17)
  ├ 个人专区与报告 (3)  └ 资金与平台安全 (14)
AI 与工具  /  纽时播报（5 篇）
```

- 生成器：`.workbuddy/gen_sidebar.py`（规则分类）+ `.workbuddy/gen_sidebars_js.py`
- 校验：`.workbuddy/check_sidebar.py` → **425 篇全覆盖、0 重复、0 失效 ID**

### 4. JSON-LD 结构化数据
- `src/theme/Root.js`：`WebSite`（含 `SearchAction`）+ `Organization`
- `src/theme/DocItem/Layout/index.js`：`TechArticle`（含 headline / description / inLanguage / image / publisher / datePublished / mainEntityOfPage）

---

## 三、重要更正

> **此前审查报告称「无 JSON-LD 结构化数据」为错误结论。**

实测 Docusaurus **已自动输出 `BreadcrumbList`**（由 `DocBreadcrumbs/StructuredData` 渲染）。本次新增的是真正缺失的 `TechArticle`、`WebSite(+SearchAction)`、`Organization`。

修正后，页面结构化数据共 **4 种类型齐备**。

---

## 四、验证结果

| 验证项 | 结果 |
|---|---|
| `yarn build` | **EXIT=0**，1m41s |
| 生成页面 | 435 个 HTML |
| `build/robots.txt` | ✅ 188 B |
| `build/img/og-default.png` | ✅ 38.8 KB，1200×630 |
| 文档页 `og:image` / `twitter:image` | ✅ |
| 文档页 `og:type=article` / `og:site_name` / `theme-color` | ✅ |
| JSON-LD 类型 | ✅ WebSite / Organization / TechArticle / BreadcrumbList |
| 首页 Hero + 3 卡片 + 页脚 | ✅ |
| 侧栏 12 个分类标签 | ✅ 全部渲染 |
| 图片 alt 合格率 | ✅ 329/329 (100%) |
| 重复 title / 正文 H1 | ✅ 0 / 0 |

---

## 五、遗留与后续

1. **`dateModified` 本地构建缺失** —— `lastUpdatedAt` 依赖 git 历史，CI 需 `fetch-depth: 0` 才完整。
2. **图片 `alt` 为规则生成** —— 覆盖 329 张，语义合理但非逐图人工核对，建议后续在关键页抽查润色。
3. **侧栏分类为启发式** —— 按文件名关键词归类，个别条目可能不理想；新增文档需手动加入对应分类（`sidebars.js` 已注释说明）。
4. **建议后续**：`theme-color` 可区分明暗主题；可加 webmanifest（PWA）；可评估引入 `slug`（但会改变全部 URL，破坏外链，需谨慎）。
