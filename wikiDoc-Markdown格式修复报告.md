# wikiDoc Markdown 格式修复报告

> 修复时间：2026-09-10
> 验证方式：真实 Markdown 解析器（micromark + GFM）+ Docusaurus 生产构建

---

## 一、根因定位：`\r\r\n` 换行损坏（表格无法渲染的真正原因）

### 现象
425 个文档中大量表格在页面上无法渲染，表现为纯文本或错乱。

### 根因
**112 个文件的行尾从标准的 `\r\n` 被写成了 `\r\r\n`（CR CR LF）。**

CommonMark / GFM 规范把 `\r`、`\n`、`\r\n` 都视为**行结束符**，因此：

```
"内容" + \r + \r + \n   =   1 行内容 + 2 个空行
```

结果：**每一行后面都凭空多出 2 个空行**。所有依赖"行连续"的语法结构全部被拆散：

- 表格：表头、分隔行、数据行之间被插入空行 → 表格不被识别
- 列表、引用块、代码块、段落：同样被拆散

实测证据（`小币种交易.mdx` 原始字节）：

```
2d 2d 2d 0d 0d 0a   =  "---" + CR + CR + LF
```

### 修复
`repair_crlf.py`（二进制读写，避免再次被转换）将 `\r\r\n` 还原为 `\r\n`，共修复 **112 个文件**。

### 影响量化（真实解析器统计）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 识别出的表格数 | 98 | **274** |
| 渲染失败的表格行 | 2464 | **0** |
| 受影响文件数 | 34 | **0** |

---

## 二、表格结构修复明细

### 1. 多行内容塞进单元格（3 张表）
作者把多行列表写进表格单元格，用孤立的 `|` 作伪结束符，Markdown 无法解析。
改写为合法表格，单元格内用 `<br/>` 连接：

- `exness交易应用-了解业绩统计选项卡.mdx`（2 张表）
- `无法出金时该如何操作.mdx`
- `账户长时间未使用会出现什么情况.mdx`（2 张表）

### 2. 标签与内容未用 `|` 分隔（6 张表）
如 `| 执行类别即时执行（…） |` 应为 `| 执行类别 | 即时执行（…） |`：

- `先锋账户.mdx`、`关于账户类型后缀.mdx`、`标准账户和先锋账户有什么不同.mdx`
- `零点账户.mdx`、`夏季交易时间.mdx`、`直盘交易.mdx`

### 3. 表格单元格内的 wiki 链接（8 处）
`[[页面|别名]]` 中的 `|` 与表格的列分隔符冲突，单元格被撕碎。
**表格内禁止使用带别名的 wiki 链接**，改为标准 Markdown 链接 `[股票](/exness-trader/股票)`。

（转义为 `\|` 不可行：插件在分词阶段先于转义处理，`\` 会混入链接目标，造成断链。）

### 4. wiki 链接写法陷阱
插件默认把链接目标中的**空格替换为连字符**，故 `[[exness-trader/股票 | 股票]]`（`|` 前带空格）会生成 `/exness-trader/股票-` 断链。
正确写法：`[[exness-trader/股票|股票]]`（`|` 前不能有空格）。

---

## 三、其它格式修复（累计五轮扫描）

| 类型 | 数量 |
|---|---|
| 标点后多余空格（1+ 空格，含 frontmatter） | 1500+ |
| 标点前多余空格 | 26 |
| 中文后接半角 `,;:` | 38 |
| `**English:**` 半角冒号 | 9 |
| 行尾空格 | 600+ |
| 连续空行（3+） | 96 |
| 标题层级跳跃（H2→H4） | 21 |
| 列表符号混用 | 8 |
| 表格列数不匹配 | 171 |
| 代码块围栏后多余空格 | 3 |
| 孤立图片说明 → 斜体 | 13 |
| 文件末尾无换行 | 68 个文件 |
| 行中连续 2+ 空格 | 372 |

---

## 四、验证

- **真实解析器**：`micromark` + `micromark-extension-gfm` 解析全部 425 个文件 → 274 张表、0 渲染失败、0 列数不一致。
- **换行符**：全站 0 个文件含 `\r\r\n`。
- **生产构建**：`yarn build` → **EXIT=0**，耗时 145.88s，生成 435 个页面、274 个 `<table>`。
- **重点文件抽查**（生成 HTML 中的 `<table>` 数量）：
  - 小币种交易：64
  - 直盘交易：7
  - 了解业绩统计选项卡：3
  - 夏季交易时间：2、账户长时间未使用：2、交易品种：2
  - 我可以使用合作伙伴链接…：1、无法出金时…：1、先锋账户：1

---

## 五、遗留（非表格，可选）

- 317 处图片 `alt` 仅为文件名、8 处 `alt` 为空 —— 需根据图片内容人工补写描述。
