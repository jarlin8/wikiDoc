#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""按行号重写坏表格"""
from pathlib import Path

ROOT = Path(r"C:\Users\Jarlin\Desktop\Github\wikiDoc")


def load(p):
    return p.read_text(encoding="utf-8")


def save(p, text):
    text = text.replace("\r\n", "\n").replace("\r", "\n")
    p.write_bytes(text.replace("\n", "\r\n").encode("utf-8"))


f1 = ROOT / "docs/exness-trader/exness交易应用-了解业绩统计选项卡.mdx"
t = load(f1)
lines = t.split("\n")

newA = [
    "| 数据 | 指标 |",
    "| --- | --- |",
    "| **净利润** | - 净利润百分比显示与同时段的上一周期相比，订单利润的变化情况。<br/>- 亏损显示选定时段内各个订单总的亏损情况。<br/>- 盈利显示某一选定时段内各个订单总的盈利情况。 |",
    "| **净值变化** | - 净值变化以%表示，显示从开始到现在的净值变化情况。<br/>- 它显示了当前的净值。 |",
    "| **订单总量** | - 订单总数百分比显示与上一相同长度的时段对比，某一时段内创建的订单总数的变化。<br/>- 盈利订单显示某一选定时段内的各个盈利订单。<br/>- 非盈利订单显示某一选定时段内的各个非盈利订单。 |",
    "| **交易量** | - 交易量百分比显示与同时段的上一周期对比，某一选定时段的订单交易量。 |",
]
newB = [
    "| 数据 | 图表描述 |",
    "| --- | --- |",
    "| **净利润** | - 在此柱形图表中，黄色栏显示在选定时段内的盈利，而灰色栏显示亏损。<br/>- 上面的黄色阴影显示在选定时段内账户处于盈利状态。<br/>- 下面的灰色阴影显示在选定时段内账户处于亏损状态。<br/>- 如果没有阴影，则表示在选定时段内无亏损/盈利。 |",
    "| **净值** | - 折线图表显示在选定时段内所选账户的净值。 |",
    "| **订单总量** | - 在此柱形图表中，黄色栏显示在选定时段内的盈利订单，而灰色栏显示非盈利订单。<br/>- 上面的灰色阴影代表某一选定时段内的盈利和非盈利订单总数。 |",
    "| **交易量** | - 黄色柱形图表栏显示某一选定时段内的交易总量。 |",
]

# 先验证边界
print("A 起始:", repr(lines[45][:30]), "| A 结束:", repr(lines[70]))
print("B 起始:", repr(lines[74][:30]), "| B 结束:", repr(lines[98]))

# 替换 B（索引 74..98）再替换 A（45..70）
lines = lines[:74] + newB + lines[99:]
lines = lines[:45] + newA + lines[71:]

save(f1, "\n".join(lines))
print("文件1 完成")
