# wikiDoc / 汇鉴 — 项目长期约定

> 站点：`wiki.ssgg.net`｜Docusaurus 3.10.2｜中文名 **汇鉴**，英文名 **Huijian**（2026-09-16 由 WikiDoc 更名）
> 本文件记录**必须遵守的约定**，改动前先读这里。

## 1. 侧栏与分类（`sidebars.js`）

- 侧栏是**显式分类树**，不是 autogenerated。**新增文档必须手动登记**，否则不显示（且不报错）。
- **一级分类一律用 `generated-index`**，slug 前缀固定 **`/category/`**（避免与文档自身 URL 如 `/exness-trader` 冲突）。

已占用的 slug（**新增前先查重**）：

| 分类 | slug |
|---|---|
| 深度指南 | `/category/guides` |
| EXNESS 客户帮助 | `/category/exness-trader` |
| EXNESS 代理帮助 | `/category/exness-agent` |
| AI 与工具 | `/category/ai-tools` |
| 纽时播报 | `/category/nytimes` |

- 二级分类**暂用显式列表**（用户决定日后按需再改）。它们是**虚拟分类**（对扁平目录的人工分组），**不能**用"建 index.mdx"实现，只能用 `generated-index`。

## 2. 内链格式（**重要，有硬约束**）

| 位置 | 格式 |
|---|---|
| **正文**（段落/列表/引用/提示框） | `[[docId\|label]]`（wiki 语法） |
| **表格单元格** | `[label](/url)`（markdown） |

**为什么表格里不能用 wiki：** 实测结论（2026-09-16）——
1. `[[id|label]]` 里的 `|` 被 GFM 表格解析器当列分隔符 → 单元格被切断 **且** 插件不再转换，页面直接输出裸 `[[...]]`
2. 转义写法 `[[id\|label]]` → 链接能生成，但 **href 尾部残留反斜杠**（`...有什么不同\`）→ 死链
3. 无别名 `[[docId]]` 在表格里可用，但 label 会显示成一长串 docId，不可接受

转换/校验脚本：`.workbuddy/unify_links.py`（表格感知）、`.workbuddy/check_guide_links.py`

## 3. 索引页

- **列表型索引页**（首页、目录总览页）→ frontmatter 加 `hide_table_of_contents: true`
  （源码依据：`DocItem/Layout/index.js:27-31,45,59` —— 为 true 时 TOC 栏**整列不输出**，正文自动占满）
- `generated-index` 页面**结构上没有 TOC 列**（由独立组件 `DocCategoryGeneratedIndexPage` 渲染，不走 `DocItem/Layout`），无需处理
- **退役**旧索引页用 `unlisted: true`（→ 侧栏移除 + `noindex`），不要删除文件

已退役：`exness-trader/exness-trader`、`exness-agent/exness-agent`、`nytimes/nytimes`（内容全是链接堆，不专业）

## 4. ⚠️ 归档/精简的致命陷阱

**「零曝光 = 可归档」这条规则对新文件不成立。**

2026-09-15 的 `archive_batch1.py` 曾把 `nytimes/pu_prime_broker_research_report.mdx`（当天新建的 40KB 报告）归档——因为 GSC 数据窗口根本覆盖不到它的创建日期。已恢复。

**规则**：归档前必须排除**文件 mtime 晚于 GSC 导出日期**的文档。已从 `reports/05-删除候选清单.csv` 剔除该项。

**归档操作三处必须同步**：① 文件移到 `_archive/` ② 分类索引页删行 ③ `sidebars.js` 删条目。漏一处 `onBrokenLinks: throw` 会让构建失败。

## 5. 构建与环境

- 无 `yarn` 时：`node node_modules/@docusaurus/core/bin/docusaurus.mjs build`
- **每一条 Bash 命令都要显式 export 完整 PATH**（含 `C:/Program Files/Git/cmd`、PortableGit `usr/bin`、node），**不要依赖 `$PATH`**（曾因 `$PATH` 未展开把 PATH 覆盖，导致 shell 工具全丢、git 找不到而构建报 `getGitFileInfo` 失败）
- 构建前**先停 `docusaurus serve`**，否则它占用 `build/` 会让构建卡死
- 构建失败后先清缓存再重跑：`node -e "for(const d of ['.docusaurus','node_modules/.cache','build']) require('fs').rmSync(d,{recursive:true,force:true})"`
- 干净重建可达 7 分钟（含 git log），**不要过早判定假死**；增量构建通常 11–25 秒
- 站点配了 `trailingSlash: false` → 产物是 `build/xxx.html` 而非 `build/xxx/index.html`
- Docusaurus 压缩 HTML 会**去掉属性引号**（`href=/xxx`），写校验正则时不要假定有引号
- `/tmp` 在不同 shell 下映射不同目录，日志可能找不到 → 直接检查 `build/` 产物

## 6. 内容写作规范（guides）

1. 页首必标「数据核验日期」
2. 来源三级标注：〔官方〕/〔第三方〕/〔站内〕
3. 拿不到权威来源的数字**不编**，只给决策框架
4. 页尾附来源列表 + CFD 风险免责声明
5. 每篇支柱页内链 ≥20 条
